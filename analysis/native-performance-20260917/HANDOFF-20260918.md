# AZ / Pi handoff — 2026-09-18 session

Branch `feat/library-session-integration-20260916`. Read this, then
`PAGE-CORRELATED-LOAD.md`, then `COMPATIBILITY-LAYER-AUDIT.md`. Everything
here was measured on the device today unless marked otherwise.

## Access

`ssh -i ~/.ssh/piflex-ssh-key -o IdentitiesOnly=yes pompu_5@192.168.1.80`.
The private key lives in `~/.ssh` (reboot-proof); `/tmp/piflex-ssh-key` is a
symlink to it for older tooling. Passwordless `sudo -n` works. The user has no
hands (speech-to-text only): **nothing may require a tap on the panel.** A
menu waiting for a tap is a dead end; automate through the kiosk loop with the
stub menu (see `local/kiosk-swap-backup/` on the Pi) and restore afterwards.
Never `systemctl restart pflx-session` while a kiosk is alive — it orphans
kiosks and blanks the screen (RESULTS.md, "duplicate kiosks").

## State of the device at handoff

One kiosk shell, real menu installed, AZ running normally with the FLX6
bridge, `share_ipc=false`, irq 111 affinity `3` (default), our audio
processes on their default cores. Dead-session leftovers cleaned. The Pi's
`run-az-interactive.py` is the older variant carrying only the library-mount
and opt-in `LAB_SHARE_IPC` changes; do not overwrite it with the branch copy
(the Pi lacks `az_paths.py`).

## What is settled

- **Steady two-deck playback has no stall**: 4,991 XDamage intervals, max
  20.6 ms, zero over 25 ms, zero storage reads, zero underruns.
- **The load hitch is renderer preemption.** In the first second after LOAD on
  the WAVEFORM page, with the other deck playing, the worst interval was
  53.7 / 72.0 / 51.1 ms across three baseline runs. The renderer (RR 1, the
  lowest RT priority in the process, CPU mask `3`) was runnable-but-preempted
  for ~90% of those windows by: the touchscreen I²C irq/111 thread (FIFO 50,
  ~5.8 kHz), `PageFiller0` (RR 11), `BufferingSched` (RR 44), `JUCE Timer`
  (RR 33), `Xwayland`, and our `mix-stream` (RR 8) / `aplay` (RR 10).
  Browser gaps of ~287 ms before LOAD are a list not repainting, not stalls.
- **MIT-SHM (`share_ipc`) is closed**: verified engaged, no CPU or frame-gap
  benefit under load. Keep it off.
- **irq 111 → CPU 3** removes that thread from the renderer's preemptors
  entirely and cut the worst gap to 42.4 ms in one trial (controls 51–72).
  Not applied; needs B/A/B repeats and a touch-latency check.
- **`mix-stream`/`aplay` → CPU 3**: mechanism works, **no benefit** (69.9 ms
  vs 59.5/50.0). Closed.
- Across seven load-span arms the worst gap is always at +0.53…+0.58 s after
  LOAD with `PageFiller0` (RR 11) as the top preemptor of the RR 1 renderer.
  The floor is firmware scheduling, not our layers.

- **Renderer raised to RR 12 (above `PageFiller0`)**: preemption by
  `PageFiller0` drops to zero, gap unchanged (59.1 vs 39.0/49.9). The
  renderer stops being starved and starts *waiting* (`S`/`D`). **Scheduling
  is not the root cause.** All three scheduling knobs closed.
- **What the renderer waits on (from blocked stacks + wakers, both arms):**
  `poll()` on its X socket, woken by `unix_write_space` (send buffer drained)
  and `sock_def_readable` (reply/event). It is blocked on the **X transport to
  Xwayland** during the load burst, while Xwayland (RT 1, same cores) is
  preempted by `PageFiller0`/`BufferingSched`. The ~40–70 ms gap *occurs*
  ~0.55 s after LOAD; nothing freezes for 0.55 s. Not a loader lock — that
  earlier claim was retracted once the stacks were read.
- **Quantified:** the renderer is *blocked* for 83–92% of the worst gap at
  either priority; preemption is 0–5% by time (it only dominated by count).
- **Attributed (strace + inode adjacency):** the renderer's poll loop watches
  an internal socketpair (fds 3/4) and **fd 20 = socket 115999**, one inode
  below Xwayland's accepted socket 116000 — the X connection. Direct peer
  proof still wants `ss -xe`/`lsof` (not installed). **Not recorded:** Xwayland's own
  switches — the capture filter was renderer-only and `-a` does not lift it.
- Next: prove the peer (`ss -xe`/`lsof`, or `strace -e poll,write -p MAIN`
  + `readlink /proc/MAIN/fd/N`), widen the filter to both PIDs, then A/B/A
  **renderer and Xwayland scheduled together**. Bare Xorg separate. Whether
  page-fill can defer so the waveform keeps drawing is a firmware question.

## Next, if the launcher side is picked up again (one knob, recovery first)

1. B/A/B repeats of the irq 111 move (the one knob that trended better),
   plus a touch-latency check; if it holds, home it in `pflx-tune`.
2. Launcher should delete its own `az-display-clock-*` / `az-live-mixer-*`
   directories on exit.
3. Do not retest renderer priority or audio affinity; both are closed above.

Otherwise the better use of the recovered material is the DSP/control
reversing: `MIXER_CONT_UCOM.mot` in the ISO is a second source for the mixer
control semantics alongside EP147. See `COMPATIBILITY-LAYER-AUDIT.md` for the
full essential-versus-avoidable split.

## Tooling that works (all in `analysis/native-performance-20260917/`)

`capture_waveform_gap.sh OUTDIR SECS` (gates: one kiosk, WAVEFORM page,
EP147 ≥ 30 %core; records XDamage + perf CPU stacks + sched_switch/wakeup/
block_rq + 10 ms page/io sampler, all CLOCK_MONOTONIC),
`attribute_waveform_gaps.py DIR [ms]`, `attribute_load_span.py DIR [top]`
(needs `commands.json` with stamped inputs). Pi-side helpers in `/tmp` are
lost on reboot: `input.py` (FIFO nav; requires `--no-controller`),
`load-two.sh` (5×enter → LOAD1 → PLAY0 → **browse** → rotate → LOAD2 → PLAY1;
`view` goes to SOURCE, not the browser), `load-span-capture.sh`, `irq-aba.sh`,
`audio-aba.sh`. `perf script` needs `-f` after `chown`. Attribution scripts
are bisect-indexed (seconds, not minutes).

## Known limits

XDamage intervals are notification gaps, not scanout. ~1,100 intervals per
60 s carry no page sample (10 ms sampler) and are excluded. Single trials
unless stated. No listening verdict was given today. `next_comm` attribution
says who ran after the renderer was switched out, which is direct for R/R+
but silent on wakeups and D states.

## Resume point, 2026-09-19 (final for this session)

Device: normal bridge session running, one kiosk, one Xwayland, real menu,
renderer RR 1, Xwayland SCHED_OTHER, irq 111 default, `share_ipc=false`, no
watchdog, leftovers cleaned. `iproute2` was installed (with the user's
relayed OK). Nothing else was installed.

Settled, with direct evidence (see PAGE-CORRELATED-LOAD.md):
- The load hitch is the renderer blocked in `poll()` for **write space on its
  X socket**; the waker is **Xwayland** (`unix_write_space`), proven by waker
  PID, not inodes.
- At default priority Xwayland is **runnable-but-preempted** through the whole
  gap by the firmware's RT load threads. Prioritising renderer+Xwayland (RR
  12, verified live) removes the secondary >25 ms cluster in all three runs
  and leaves the primary ~35–45 ms gap.
- With X no longer starved, its residual waits are **V3D GPU buffer creation
  (`v3d_create_bo_ioctl`, MMU flush) and GPU fence waits**, occurring only in
  the first second after LOAD. That is the glamor/compositor path stock AZ
  does not have.

Closed: share_ipc; renderer-only priority; audio affinity; irq 111 alone
(trend only). Partial lever, unapplied: joint renderer+Xwayland priority.

Also (2026-09-19): Xwayland `-glamor off` A/B/A'd at default priorities and
on the joint RR 12 baseline showed no consistent benefit — a poor candidate
to enable, not proof that X's GPU waits are irrelevant
(PAGE-CORRELATED-LOAD.md, last two sections).

Next, in order:
1. Validate joint priority (promising, not validated: 0–2 gaps >25 ms on
   load vs 3–7, but one load per arm): repeat the comparison over several
   loads inside one session, compare startup underruns against normal
   startups, measure touch response. Only then a `pflx-tune` home. Not
   promoted.
2. **Bare Xorg** — packages approved and installed 2026-09-19; two six-load
   arms (6, 7 long intervals against 8–10 for five Xwayland default arms,
   renderer 39 %core against 44–45) and a 16-minute sustained pair with the
   same steady-state cadence on both servers and no freezes
   (PAGE-CORRELATED-LOAD.md, last section). Next: the sustained pair with
   `LOAD_WAIT=12` (running/ran unattended, `local/sustained-*.log`), joint
   priority on the bare-Xorg baseline, an hour-scale session, then decide
   on a third kiosk mode (sway stopped, Xorg on the DSI KMS device,
   `external_display`). Helpers: `tools/bare-xorg/`.
5. **Kyle review timer sites** (`KYLE-AZ-60FPS-REVIEW.md`): the two extra
   `startTimer(33)` call sites (0x24fac4c, 0x24f6b34) are an opt-in
   `timer_sites` session key (overlay patches 9–10, verified byte-exact on
   the pinned image, off by default). Test separately from bare Xorg:
   `CAPTURE=/tmp/multi-load-capture.sh LOADS=6 sh /tmp/scheduling-aba.sh
   timersites` — the arm summary prints the overlay manifest (patches 8 vs
   10). Judge on damage cadence during load, EP147 %core, underruns; timer
   ownership is still not traced, so a gain needs a mechanism before it is
   promoted.
4. **Slow-track loads**: particular tracks keep the loading cadence 1–4 s
   after LOAD and spike the same loads every arm; find what differs about
   them (ANLZ/artwork size, cache state) before treating the spike as a
   presentation problem.
3. Joint priority + bare Xorg have not been combined; if both hold up, that
   is the next single-knob A/B/A (bare Xorg baseline, joint on/off).
Helpers: `tools/` (all state-dependent; README). Do not loop `python3 -
<<EOF` over a perf pipe; write perf output to a file first.

Launcher patching on the Pi: its `run-az-interactive.py` uses **one-space
indentation** and the Xwayland `Popen` sits inside a `with` block. A patch
that inserts lines at column 0 still *parses* but moves the `Popen` outside
the `with`, so the log file is closed and every launch dies with "Player/audio
exited" and an empty `launcher.log`. That happened on 2026-09-19 and burned
one A/B/A. Verify structurally (AST: the `xv=` assignment must be inside the
`With` node), not with `ast.parse` alone, and keep the pre-patch copy under
`local/kiosk-swap-backup/`.

`xwayland_glamor` (`gl|es|off|null`) is now an opt-in session key; the Pi's
Xwayland 24.1.6 honours `-glamor off` (software rendering, no V3D buffer
objects). The `noglamor` knob of `tools/scheduling-aba.sh` A/B/As it.

## Resume point, 2026-09-19 ~11:26 UTC

The Pi went down ("System is going down", then no ping for 200 s — a
power-off, not by this session) while the last no-controller arm session
was being replaced by a normal controller session. State on the next boot:
kiosk files were verified restored before the shutdown (real menu, real
`pflx-az-session`, real `start-pflx-kiosk`); the menu falls to BiteDJ after
its countdown, so AZ needs a tap on the menu (or the stub-menu trick). The
Pi checkout carries `pi/session.py` and `run-az-interactive.py` from
`5ae083c` (external_display, SIGTERM unwind, stale-overlay sweep, signal-safe
child registration); `local/session-library.json` has no `external_display`
key (defaults to null), `share_ipc` false, `xwayland_glamor` null. Xorg
packages are installed; nothing starts Xorg on its own. Pre-patch launcher
copies: `local/kiosk-swap-backup/run-az-interactive.py.pre-{glamor,sigterm,external}`.
